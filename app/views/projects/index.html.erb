<% content_for :title, "案件一覧" %>

<div class="w-full">
  <% if notice.present? %>
    <p class="py-2 px-3 bg-green-50 mb-5 text-green-500 font-medium rounded-md inline-block" id="notice"><%= notice %></p>
  <% end %>

  <div class="flex justify-between items-center mb-6">
    <h1 class="font-bold text-2xl text-gray-800">案件一覧</h1>
    <%= link_to "新規登録", new_project_path, class: "rounded-md px-4 py-2 bg-blue-600 hover:bg-blue-500 text-white font-medium" %>
  </div>

  <% if @projects.any? %>
    <div class="bg-white shadow-sm rounded-lg overflow-hidden">
      <table class="min-w-full divide-y divide-gray-200">
        <thead class="bg-gray-50">
          <tr>
            <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase">案件名</th>
            <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase">クライアント</th>
            <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase">単価</th>
            <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase">勤務形態</th>
            <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase">期間</th>
            <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase">状態</th>
            <th class="px-6 py-3 text-right text-xs font-medium text-gray-500 uppercase">操作</th>
          </tr>
        </thead>
        <tbody class="bg-white divide-y divide-gray-200">
          <% @projects.each do |project| %>
            <tr class="hover:bg-gray-50">
              <td class="px-6 py-4 whitespace-nowrap">
                <%= link_to project.name, project, class: "text-blue-600 hover:text-blue-800 font-medium" %>
              </td>
              <td class="px-6 py-4 whitespace-nowrap text-gray-700"><%= project.client_name %></td>
              <td class="px-6 py-4 whitespace-nowrap text-gray-700"><%= formatted_unit_price(project.unit_price) %></td>
              <td class="px-6 py-4 whitespace-nowrap text-gray-700"><%= work_style_label(project) %></td>
              <td class="px-6 py-4 whitespace-nowrap text-gray-700">
                <%= project.start_date %> 〜 <%= project.end_date %>
              </td>
              <td class="px-6 py-4 whitespace-nowrap">
                <span class="px-2 py-1 text-xs font-medium rounded-full <%= status_badge_class(project) %>">
                  <%= status_label(project) %>
                </span>
              </td>
              <td class="px-6 py-4 whitespace-nowrap text-right space-x-2">
                <%= link_to "編集", edit_project_path(project), class: "text-gray-600 hover:text-gray-800" %>
                <%= button_to "削除", project, method: :delete, class: "text-red-600 hover:text-red-800 cursor-pointer", form: { data: { turbo_confirm: "この案件を削除しますか？" } } %>
              </td>
            </tr>
          <% end %>
        </tbody>
      </table>
    </div>
  <% else %>
    <div class="bg-white shadow-sm rounded-lg p-10 text-center text-gray-500">
      案件が登録されていません。「新規登録」から案件を追加してください。
    </div>
  <% end %>
</div>
