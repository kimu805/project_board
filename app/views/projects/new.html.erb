<% content_for :title, "案件登録" %>

<div class="max-w-2xl">
  <nav class="mb-8 flex items-center gap-2 text-sm">
    <%= link_to "案件一覧", projects_path, class: "text-gray-500 hover:text-warm-400 transition-colors" %>
    <span class="text-gray-700">/</span>
    <span class="text-gray-300">新規登録</span>
  </nav>

  <div class="mb-8">
    <p class="text-xs font-display tracking-widest text-warm-500 uppercase mb-2">New Project</p>
    <h1 class="font-display font-extrabold text-3xl text-white">案件登録</h1>
  </div>

  <div class="bg-navy-900 border border-white/5 rounded-2xl p-6 sm:p-8">
    <%= render "form", project: @project %>
  </div>
</div>
